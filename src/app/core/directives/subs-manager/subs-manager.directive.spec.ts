import { SubsManagerDirective } from './subs-manager.directive';

describe('SubsManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new SubsManagerDirective();
    expect(directive).toBeTruthy();
  });
});
